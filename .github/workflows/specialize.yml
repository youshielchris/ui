name: blank

on:
  push:

jobs:
  perfect:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        newly: ["prompt", "enroll", "appointment", "intelligent", "craft"]
        engine: ["dye", "anything", "color", "pillar"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar VL
